# Server Port & Environment
PORT=5000
NODE_ENV=development

# Database Connection String (Neon Hosted PostgreSQL)
# Format: postgresql://<user>:<password>@<ep-hostname>.neon.tech/<dbname>?sslmode=require
DATABASE_URL="postgresql://user:password@ep-xyz.region.aws.neon.tech/servmate?sslmode=require"

# JWT Authentication
JWT_SECRET=servmate_super_secret_jwt_key_placement_ready_2026
JWT_EXPIRES_IN=7d

# CORS Allowed Origins
CLIENT_URL=http://localhost:5173

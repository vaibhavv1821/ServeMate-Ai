# 16 - Deployment Architecture

**Current Phase Status**: Implemented (Phase 1 Build & Environment Scaffolding)

---

## 1. Planned Deployment Infrastructure (`STATUS: PLANNED`)
- **Frontend**: Vercel (Static Web App deployment for React SPA built with Vite).
- **Backend**: Render (Node.js web service running Express API).
- **Database**: Hosted PostgreSQL (Neon serverless PostgreSQL database with SSL connection pooling).
